diff --git a/resources/views/admin/dashboard.blade.php b/resources/views/admin/dashboard.blade.php
index f934d57..ef1b86b 100644
--- a/resources/views/admin/dashboard.blade.php
+++ b/resources/views/admin/dashboard.blade.php
@@ -459,10 +459,8 @@ function closeModal(key) {
                 }
             </script>
 
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 {{-- Mobile sidebar overlay --}}
 <div id="mobileSidebar" class="md:hidden fixed inset-0 z-50 hidden">
     <div class="w-[240px] h-full bg-white overflow-y-auto">
@@ -519,15 +517,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/admin/data-neraca.blade.php b/resources/views/admin/data-neraca.blade.php
index 778b132..2583b69 100644
--- a/resources/views/admin/data-neraca.blade.php
+++ b/resources/views/admin/data-neraca.blade.php
@@ -216,11 +216,11 @@ class="w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium
                     <p class="text-sm text-slate-500">Seluruh data neraca pangan Kota Kediri</p>
                 </div>
                 <div class="flex gap-2">
-                    <a href="{{ Route::has('admin.data.export.excel') ? route('admin.data.export.excel', request()->query()) : '#' }}" class="flex items-center gap-1.5 px-3 py-2 rounded-lg border border-blue-200 text-blue-600 text-sm hover:bg-blue-100 transition-colors">
+                    <a href="{{ Route::has('admin.data.export.excel') ? route('admin.data.export.excel', request()->query()) : '#' }}" class="flex items-center gap-1.5 px-3 py-2 rounded-lg border border-blue-200 text-blue-600 text-sm hover:bg-blue-100 transition-colors" target="_blank" rel="noopener">
                         <svg xmlns="http://www.w3.org/2000/svg" class="w-[14px] h-[14px]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12 12 16.5m0 0L7.5 12m4.5 4.5V3"/></svg>
                         Excel
                     </a>
-                    <a href="{{ Route::has('admin.data.export.pdf') ? route('admin.data.export.pdf', request()->query()) : '#' }}" class="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-semibold text-white" style="background-color:#2563EB;">
+                    <a href="{{ Route::has('admin.data.export.pdf') ? route('admin.data.export.pdf', request()->query()) : '#' }}" class="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-semibold text-white" style="background-color:#2563EB;" target="_blank" rel="noopener">
                         <svg xmlns="http://www.w3.org/2000/svg" class="w-[14px] h-[14px]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12 12 16.5m0 0L7.5 12m4.5 4.5V3"/></svg>
                         PDF
                     </a>
@@ -385,10 +385,8 @@ function openDelete(item) {
                     document.getElementById('modalDelete').classList.remove('hidden');
                 }
             </script>
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 {{-- ============ MODAL DETAIL ============ --}}
 <div id="modalDetail" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/40 backdrop-blur-sm p-4" onclick="if(event.target===this) this.classList.add('hidden')">
     <div class="bg-white rounded-2xl shadow-2xl w-full max-w-md max-h-[90vh] overflow-y-auto">
@@ -505,15 +503,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/admin/laporan.blade.php b/resources/views/admin/laporan.blade.php
index 2b8bcaa..ebcb448 100644
--- a/resources/views/admin/laporan.blade.php
+++ b/resources/views/admin/laporan.blade.php
@@ -257,11 +257,11 @@ class="w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium
                     <p class="text-sm text-slate-500">Dinas Ketahanan Pangan dan Pertanian Kota Kediri</p>
                 </div>
                 <div class="flex gap-2">
-                    <a href="{{ route('admin.laporan.export.excel', request()->query()) }}" class="flex items-center gap-1.5 px-4 py-2 rounded-xl text-sm font-semibold border-2 border-blue-300 text-blue-700 hover:bg-blue-100 transition-colors">
+                    <a href="{{ route('admin.laporan.export.excel', request()->query()) }}" class="flex items-center gap-1.5 px-4 py-2 rounded-xl text-sm font-semibold border-2 border-blue-300 text-blue-700 hover:bg-blue-100 transition-colors" target="_blank" rel="noopener">
                         <svg xmlns="http://www.w3.org/2000/svg" class="w-[15px] h-[15px]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12 12 16.5m0 0L7.5 12m4.5 4.5V3"/></svg>
                         Ekspor Excel
                     </a>
-                    <a href="{{ route('admin.laporan.cetak', request()->query()) }}" class="flex items-center gap-1.5 px-4 py-2 rounded-xl text-sm font-semibold text-white shadow-sm" style="background-color:#2563EB;">
+                    <a href="{{ route('admin.laporan.cetak', request()->query()) }}" class="flex items-center gap-1.5 px-4 py-2 rounded-xl text-sm font-semibold text-white shadow-sm" style="background-color:#2563EB;" target="_blank" rel="noopener">
                         <svg xmlns="http://www.w3.org/2000/svg" class="w-[15px] h-[15px]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12 12 16.5m0 0L7.5 12m4.5 4.5V3"/></svg>
                         Cetak PDF
                     </a>
@@ -635,10 +635,8 @@ function renderCharts() {
                 }
             </script>
 
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 <script>
     function tandaiNotifDibaca(el, url) {
         if (!url || el.dataset.done === '1') return;
@@ -677,15 +675,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/admin/manage-user.blade.php b/resources/views/admin/manage-user.blade.php
index ec1a4fa..c51237c 100644
--- a/resources/views/admin/manage-user.blade.php
+++ b/resources/views/admin/manage-user.blade.php
@@ -323,10 +323,8 @@ class="p-1.5 rounded-lg border border-red-100 text-red-400 hover:bg-red-100 tran
                     </div>
                 @endif
             </div>
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 {{-- ============ MODAL TAMBAH PENGGUNA ============ --}}
 <div id="modalTambah" class="{{ $errors->has('login_id') ? '' : 'hidden' }} fixed inset-0 z-50 flex items-center justify-center bg-black/40 backdrop-blur-sm p-4" onclick="if(event.target===this) this.classList.add('hidden')">
     <div class="bg-white rounded-2xl shadow-2xl w-full max-w-sm p-6 space-y-4">
@@ -492,15 +490,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/admin/master-komoditas.blade.php b/resources/views/admin/master-komoditas.blade.php
index 96bb5d7..0c55988 100644
--- a/resources/views/admin/master-komoditas.blade.php
+++ b/resources/views/admin/master-komoditas.blade.php
@@ -295,10 +295,8 @@ class="p-1.5 rounded-lg border border-red-100 text-red-400 hover:bg-red-100 tran
                     </div>
                 @endif
             </div>
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 {{-- ============ MODAL TAMBAH KOMODITAS ============ --}}
 <div id="modalTambah" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/40 backdrop-blur-sm p-4" onclick="if(event.target===this) this.classList.add('hidden')">
     <div class="bg-white rounded-2xl shadow-2xl w-full max-w-sm p-6 space-y-4">
@@ -418,15 +416,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/admin/notifikasi.blade.php b/resources/views/admin/notifikasi.blade.php
index 2f2870d..ddfe615 100644
--- a/resources/views/admin/notifikasi.blade.php
+++ b/resources/views/admin/notifikasi.blade.php
@@ -258,10 +258,8 @@ class="w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium
                 @endforelse
             </div>
 
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 <script>
     function tandaiNotifDibaca(el, url) {
         if (!url || el.dataset.done === '1') return;
@@ -300,15 +298,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/operator/dashboard.blade.php b/resources/views/operator/dashboard.blade.php
index 1d40b5c..c73dd49 100644
--- a/resources/views/operator/dashboard.blade.php
+++ b/resources/views/operator/dashboard.blade.php
@@ -387,10 +387,8 @@ function closeModal(key) {
                 }
             </script>
 
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 <script>
     function tandaiNotifDibaca(el, url) {
         if (!url || el.dataset.done === '1') return;
@@ -429,15 +427,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/operator/data-neraca.blade.php b/resources/views/operator/data-neraca.blade.php
index e106c57..465b2e9 100644
--- a/resources/views/operator/data-neraca.blade.php
+++ b/resources/views/operator/data-neraca.blade.php
@@ -381,10 +381,8 @@ class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg border border-blu
                     </div>
                 @endif
             </div>
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 {{-- ============ MODAL DETAIL (read-only, status valid / menunggu) ============ --}}
 <div id="modalDetail" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/40 backdrop-blur-sm p-4" onclick="if(event.target===this) this.classList.add('hidden')">
     <div class="bg-white rounded-2xl shadow-2xl w-full max-w-md max-h-[90vh] overflow-y-auto">
@@ -607,15 +605,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/operator/input-neraca.blade.php b/resources/views/operator/input-neraca.blade.php
index 3e5da91..4a69629 100644
--- a/resources/views/operator/input-neraca.blade.php
+++ b/resources/views/operator/input-neraca.blade.php
@@ -364,10 +364,8 @@ class="w-full flex items-center justify-center gap-2 px-4 py-3 rounded-xl text-s
             </form>
 
             @endif
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 <script>
     (function () {
         const stokAwalUrl = @json(route('operator.input.stok-awal'));
@@ -471,15 +469,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/operator/laporan.blade.php b/resources/views/operator/laporan.blade.php
index 22e7b63..0086352 100644
--- a/resources/views/operator/laporan.blade.php
+++ b/resources/views/operator/laporan.blade.php
@@ -262,7 +262,7 @@ class="w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium
                     <p class="text-sm text-slate-500">Data yang diinput oleh {{ $firstName }}</p>
                 </div>
                 <a href="{{ Route::has('operator.laporan.cetak') ? route('operator.laporan.cetak', request()->query()) : '#' }}"
-                   class="flex items-center gap-1.5 px-4 py-2 rounded-xl text-sm font-semibold text-white shadow-sm hover:shadow-md transition-all" style="background-color:#2563EB;">
+                   class="flex items-center gap-1.5 px-4 py-2 rounded-xl text-sm font-semibold text-white shadow-sm hover:shadow-md transition-all" style="background-color:#2563EB;" target="_blank" rel="noopener">
                     <svg xmlns="http://www.w3.org/2000/svg" class="w-[15px] h-[15px]" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 005.25 21h13.5A2.25 2.25 0 0021 18.75V16.5M16.5 12 12 16.5m0 0L7.5 12m4.5 4.5V3"/></svg>
                     Cetak PDF
                 </a>
@@ -394,10 +394,8 @@ class="flex items-center gap-1.5 px-4 py-2 rounded-xl text-sm font-semibold text
                     </div>
                 @endif
             </div>
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 <script>
     function tandaiNotifDibaca(el, url) {
         if (!url || el.dataset.done === '1') return;
@@ -436,15 +434,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/operator/notifikasi.blade.php b/resources/views/operator/notifikasi.blade.php
index bfd81e5..352be3e 100644
--- a/resources/views/operator/notifikasi.blade.php
+++ b/resources/views/operator/notifikasi.blade.php
@@ -266,10 +266,8 @@ class="w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium
                     <div class="px-5 py-10 text-center text-slate-400 text-sm">Belum ada notifikasi.</div>
                 @endforelse
             </div>
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 <script>
     function tandaiNotifDibaca(el, url) {
         if (!url || el.dataset.done === '1') return;
@@ -308,15 +306,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/verifikator/dashboard.blade.php b/resources/views/verifikator/dashboard.blade.php
index 1dbf3c1..70af0e7 100644
--- a/resources/views/verifikator/dashboard.blade.php
+++ b/resources/views/verifikator/dashboard.blade.php
@@ -348,10 +348,8 @@ function closeModal(key) {
                 }
             </script>
 
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 <script>
     function tandaiNotifDibaca(el, url) {
         if (!url || el.dataset.done === '1') return;
@@ -390,15 +388,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/verifikator/data-menunggu-verif.blade.php b/resources/views/verifikator/data-menunggu-verif.blade.php
index 31217b3..a32c3e5 100644
--- a/resources/views/verifikator/data-menunggu-verif.blade.php
+++ b/resources/views/verifikator/data-menunggu-verif.blade.php
@@ -276,10 +276,8 @@ class="flex-shrink-0 flex items-center gap-1.5 px-4 py-2 rounded-xl text-sm font
                 </div>
             @endif
 
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 <script>
     function tandaiNotifDibaca(el, url) {
         if (!url || el.dataset.done === '1') return;
@@ -318,15 +316,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/verifikator/notifikasi.blade.php b/resources/views/verifikator/notifikasi.blade.php
index 9f4f10a..5d901e6 100644
--- a/resources/views/verifikator/notifikasi.blade.php
+++ b/resources/views/verifikator/notifikasi.blade.php
@@ -269,10 +269,8 @@ class="w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm font-medium
                     <div class="px-5 py-10 text-center text-slate-400 text-sm">Belum ada notifikasi.</div>
                 @endforelse
             </div>
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 <script>
     function tandaiNotifDibaca(el, url) {
         if (!url || el.dataset.done === '1') return;
@@ -311,15 +309,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/verifikator/riwayat.blade.php b/resources/views/verifikator/riwayat.blade.php
index e6926e5..8f20582 100644
--- a/resources/views/verifikator/riwayat.blade.php
+++ b/resources/views/verifikator/riwayat.blade.php
@@ -319,10 +319,8 @@ class="inline-flex items-center gap-1 px-2.5 py-1 rounded-lg border border-blue-
                 </div>
             </div>
 
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 {{-- ============ MODAL DETAIL RIWAYAT (dibuka di dalam halaman, tanpa pindah halaman, dengan blur background) ============ --}}
 <div id="riwayatModalOverlay" class="hidden fixed inset-0 z-50 items-center justify-center bg-black/40 backdrop-blur-sm p-4"
      onclick="if(event.target===this) closeRiwayatDetail()">
@@ -483,13 +481,19 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 
     function openRiwayatDetail(id) {
         var tpl = document.getElementById('riwayat-detail-' + id);
@@ -517,7 +521,12 @@ function closeRiwayatDetail() {
         if (e.key === 'Escape') closeRiwayatDetail();
     });
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
diff --git a/resources/views/verifikator/verifikasi-detail.blade.php b/resources/views/verifikator/verifikasi-detail.blade.php
index bd6130e..74b9f5a 100644
--- a/resources/views/verifikator/verifikasi-detail.blade.php
+++ b/resources/views/verifikator/verifikasi-detail.blade.php
@@ -290,10 +290,8 @@ class="flex-1 py-3 rounded-xl text-sm font-semibold text-white shadow-sm hover:s
                     </form>
                 </div>
             </div>
-        </main>
-    </div>
-</div>
-
+        
+<!-- ====== Modal & script khusus halaman ini (dipindah ke dalam #pageContent supaya ikut ter-refresh saat navigasi SPA - lihat runScripts() di spa-nav.js) ====== -->
 <script>
     function tandaiNotifDibaca(el, url) {
         if (!url || el.dataset.done === '1') return;
@@ -332,15 +330,26 @@ function toggleNotifDropdown(e) {
         if (!dropdown) return;
         dropdown.classList.toggle('hidden');
     }
-    document.addEventListener('click', function (e) {
-        var dropdown = document.getElementById('notifDropdown');
-        if (!dropdown || dropdown.classList.contains('hidden')) return;
-        if (!dropdown.parentElement.contains(e.target)) {
-            dropdown.classList.add('hidden');
-        }
-    });
+    // Guard: script ini sekarang dieksekusi ulang setiap kali halaman
+    // dibuka lewat SPA nav (karena dipindah ke dalam #pageContent), jadi
+    // pasang listener document sekali saja supaya tidak menumpuk dobel.
+    if (!window.__notifDropdownOutsideClickBound) {
+        window.__notifDropdownOutsideClickBound = true;
+        document.addEventListener('click', function (e) {
+            var dropdown = document.getElementById('notifDropdown');
+            if (!dropdown || dropdown.classList.contains('hidden')) return;
+            if (!dropdown.parentElement.contains(e.target)) {
+                dropdown.classList.add('hidden');
+            }
+        });
+    }
 </script>
-    <script src="{{ asset('js/sidebar-toggle.js') }}"></script>
+    
+</main>
+    </div>
+</div>
+
+<script src="{{ asset('js/sidebar-toggle.js') }}"></script>
     <script src="{{ asset('js/spa-nav.js') }}"></script>
 </body>
 </html>
\ No newline at end of file
